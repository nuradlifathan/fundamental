// Convert Boolean values to strings "Yes" : "No"

const boolToolWord = (bool) => {
  const str = bool === true ? "Yes" : "No"
  return str
}
console.log(boolToolWord("Yes"))

// Using If else

/*const boolToolWord = (bool) => {
  if (bool) {
    return "Yes"
  } else {
    return "No"
  }
}*/
