// Fastest Way
const duplicate = (num) => {
  //   return Number(num.toString().split("").sort().reverse().join(""))
    let hashmap = {}

    for
}

// console.log(duplicate(2468013))
module.exports = duplicate
